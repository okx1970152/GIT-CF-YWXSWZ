# 内容生产 ↔ 前端 字段对照表

> **站点策略（已定）**：小说侧**纯文字**，不设小说封面图、不加载远程插图资源，以保证访问与加载速度。`cover` / `hero` 在数据中保留为空字符串占位，**前端不渲染任何封面或章节大图**。  
> 状态：**✅** 已对齐 · **⚠️** 非展示用途（SEO 镜像/预留） · **—** 不适用本站点策略

---

## 一、`info/index.md`（`novelInfoSchema`）

| 字段 | 说明 |
| --- | --- |
| ✅ `title` | 生产写入中文书名；列表展示优先 `title_en`，中文多用于素材与合规 |
| ✅ `title_en` | `getDisplayNovelTitle()` |
| ✅ `author` / `category` / `novel_id` | 卡片、目录、路由 |
| ✅ `summary` / `desc` | `getNovelSummary()`（优先 `summary`） |
| ✅ `total_chapters` / `status` / `updated_at` / `tags` | 卡片、目录、排序 |
| ✅ `cover` / `hero` | **始终可为空**；不设 `<img>`，不占带宽 **（对齐策略）** |
| ✅ `featured` / `hot` | `getHotNovels` 等；刷新 info 时 **保留原值**，并与 `novel.json` 同步 |
| ⚠️ `ranking` | 刷新时保留数值；**当前无排序 UI**（预留字段，零额外请求） |

---

## 二、`meta/novel.json`（`NovelMeta`）

| 字段 | 说明 |
| --- | --- |
| ✅ `keywords` / `seo_title` / `meta_description` | 目录页 `generateMetadata` |
| ✅ `og_*` / `twitter_*` | 目录页 Open Graph / Twitter |
| ✅ `tags` | `keywords` 缺省时的回退 |
| ✅ `featured` / `hot` | 与 `info/index.md` **同步写入**（刷新小说信息后一致） |
| ⚠️ `title` / `title_en` / `slug` / `category` / `summary` / `status` | 与 frontmatter **同源镜像**，便于外部工具读取；**页面渲染仍以 frontmatter 为准** |
| ⚠️ `chapter_count` | 与 `total_chapters` 同源；列表展示仍读 frontmatter |
| ⚠️ `updated_at` | 元数据时间戳；列表新鲜度仍主要用 frontmatter `updated_at` |

---

## 三、`chapters/<chapter_no>-<slug>.md`

| 字段 | 说明 |
| --- | --- |
| ✅ `title` / `chapter_no` / `published_at` / `updated_at` | 索引 + 阅读页 / JSON-LD |
| ✅ `word_count` | 生产写入；**索引** `wordCount`（无 YAML 时由正文估算）；**目录列表 + 章节页**轻量展示「N words」（纯文本） |
| ✅ 正文 Markdown | `MainContent` |

---

## 四、`annotations/<chapter_no>.md`

| 字段 | 说明 |
| --- | --- |
| ✅ frontmatter `title` / `chapter_no` / `updated_at` | 侧栏标题；`updated_at` 不入索引类型但存在于文件中 |
| ✅ 正文四段导读 | 侧栏 HTML |
| ✅ 文末 `Related Topics:` | 由 `guide_tags` **自动生成**尾部，与 `parseRelatedTopics` 兼容，并与 meta 标签一致 |

---

## 五、`meta/<chapter_no>.json`（`ChapterMeta`）

| 字段 | 说明 |
| --- | --- |
| ✅ `chapter_no` | 已列入 TS 类型 |
| ✅ `chapter_keywords` / `guide_tags` | SEO keywords、侧栏 topics、搜索摘录 |
| ✅ `chapter_seo_title` / `chapter_meta_description` / `og_*` / `twitter_*` | 章节 metadata 全链 |
| ⚠️ `chapter_slug` / `chapter_code` / `chapter_title_en` | **SEO、文件名与调试**；页面 H1 使用正文 `title`（符合阅读体验） |
| ⚠️ `updated_at` | 元数据；未单独占一行 UI（避免噪音） |

---

## 六、仅生产端（设计如此）

| 产出 | 说明 |
| --- | --- |
| ✅ `00-提示词前缀/*.md` | 仅 `novel_producer` 使用 |
| ✅ `6-增量术语与临时规则.md` | 影响后续模型调用，非站点展示 |
| ✅ `runtime/progress.json` | 本地进度，不发布 |

---

## 七、结论

- **纯文字站**：无封面图、无小说级 hero 图；仅文字词数等轻量信息可展示。  
- **契约对齐**：`word_count` 已索引并展示；`Related Topics` 与 `guide_tags` 同源；`featured/hot` 刷新时保留并与 `novel.json` 同步。  
- **副本目录**：若以 `999-英文小说网站/` 为主仓库，其它拷贝目录需自行同步代码与内容。

---

*以下为可选勾选列（自行打勾验收）：*

| 验收项 | 勾选 |
| --- | --- |
| 索引含 `wordCount` | [ ] |
| 目录/章节页可见 word 计数（文字） | [ ] |
| 刷新小说信息后 featured/hot 未被误清空 | [ ] |
| 导读尾部 Related Topics 与侧栏标签一致 | [ ] |
